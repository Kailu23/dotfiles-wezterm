# Config files for WezTerm

These config files are for WezTerm, which was chosen because of cross-platform availability.

## Setup

### Windows

It requires adding `bash.exe`  (Windows) to your `Path`  enviroment variable
Path to `bash.exe` is `C:\Program Files\Git\bin`
